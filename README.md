# Image to ASCII
Turn an image to ASCII Art

# Usage
Requires <a href="https://www.python.org">Python</a> and <a href="https://pypi.org/project/pillow/">Pillow library</a>.

You can either just run it and enter the path to your image during the prompt or pass it as a parameter.
